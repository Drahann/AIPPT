# ============ secrets (never commit) ============
**/keys.local.json
*.key

# ============ giant image libraries (regenerable; uploaded separately) ============
# Reference corpus (~800 parsed templates, ~12 GB) and the function-decoration library
# (~11 GB). Too large for a plain Git repo — provided out-of-band.
study/corpus/
study/assets_lib/

# ============ external / not part of this project ============
# raw .pptx template library (input)
挑战杯*/
# upstream reference project (its own repo)
ppt-master/
# qwen-code local config (skill mirror lives in .claude/skills)
.qwen/
# standalone archive of study/ (large, regenerable)
study.zip

# ============ internal scratch / regenerable ============
study/runs/_transcripts/
study/loop/
study/build_loop/
study/contact_sheets/
study/renders/
study/metrics/
study/assets/

# ============ python / misc caches ============
**/__pycache__/
*.pyc
*.tmp
*.log

# ============ local IDE/editor ============
.vscode/
.idea/

# ============ .claude: keep skills, ignore local settings/logs ============
.claude/*
!.claude/skills
